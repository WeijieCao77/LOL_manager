node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
*.log

# never commit API keys
.env
.env.local

# python bytecode
__pycache__/
*.pyc

# scrape caches: raw vlr/liquipedia responses, rebuildable
scripts/cache/

# local experiments, not the game's build
dist-minitool/
val-manager-minitool.zip

# the server-side engine bundle, built by `npm run build:server`
dist-server

# reference clones of Val_Manager / LOL_breaker — read-only, not part of this game
_ref/

# research caches, rebuildable
analysis/lol/cache/

audit_report.txt
audit_rerun.txt
